######################################################################
# Propagation: com.samsung.unifiedtp
######################################################################

JARs:                 telephony-common
Classes:              1
Occurrences:          1
Usage types:          invoke
Enforcement patterns: allowlist_check, early_return, string_compare, system_property_read
Security tags:        APPOPS, PKG_QUERY

────────────────────────────────────────────────────────────
Occurrence #1
────────────────────────────────────────────────────────────
  JAR:      telephony-common
  Class:    com.android.internal.telephony.dataconnection.DcTracker
  Method:   <com.android.internal.telephony.dataconnection.DcTracker: void onDataSetupComplete(com.android.internal.telephony.dataconnection.ApnContext,boolean,int,int,int)>
  Line:     41181
  Register: v1 := "com.samsung.unifiedtp"
  Usage:    [invoke] invoke-virtual {p2, v1}, Landroid/content/Intent;->setPackage(Ljava/lang/String;)Landroid/content/In
  Patterns: allowlist_check, string_compare, early_return, system_property_read
  Flow:     [invoke] android.content.Intent.setPackage() → [reassigned] v1 overwritten

  CALLERS (who invokes this method):
      ← DcTracker.handleMessage()

  CALLEES (what this method invokes):
      → Notification$BigTextStyle.bigText()
      → <android.app.Notification$BigTextStyle: void <init>()>
      → Notification$Builder.build()
      → Notification$Builder.setAutoCancel()
      → Notification$Builder.setColor()
      → Notification$Builder.setContentText()
      → Notification$Builder.setContentTitle()
      → Notification$Builder.setSmallIcon()
      → Notification$Builder.setStyle()
      → Notification$Builder.setTicker()
      → Notification$Builder.setWhen()
      → <android.app.Notification$Builder: void <init>(android.conte
      → NotificationManager.notify()
      → Context.registerReceiver()
      → Context.getResources()
      → Context.getText()
      → Context.getSystemService()
      → Context.sendStickyBroadcastAsUser()
      → Context.unregisterReceiver()
      → Intent.putExtra()
      → Intent.putExtra()
      → Intent.setPackage()
      → <android.content.Intent: void <init>(java.lang.String)>
      → <android.content.IntentFilter: void <init>(java.lang.String)
      → Resources.getColor()
      → LinkProperties.getInterfaceName()
      → PersistableBundle.getBoolean()
      → SystemProperties.getInt()
      → DataFailCause.isEventLoggable()
      → DataFailCause.isRadioRestartFailure()
      → DataFailCause.toString()
      → ServiceState.getRilMobileDataRadioTechnology()
      → TelephonyManager.getNetworkType()
      → TelephonyManager.getNetworkOperatorName()
      → ApnSetting.getApnTypesBitmaskFromString()
      → ApnSetting.getId()
      → ApnSetting.getApnName()
      → DataCallResponse.failureModeToString()
      → TextUtils.equals()
      → EventLog.writeEvent()
      → CarrierSignalAgent.notifyCarrierSignalReceivers()
      → LocalLog.log()
      → Phone.getContext()
      → Phone.getCarrierSignalAgent()
      → Phone.getPhoneId()
      → Phone.getSubId()
      → Phone.getMobileProvisioningUrl()
      → PhoneInternalInterface.getServiceState()
      → TelephonyFeatures.supportDualLte()
      → TelephonyFeatures.getMainOperatorName()
      → AlwaysOnPdn.isAlwaysOnPdnRequiredByModem()
      → AlwaysOnPdn.handleAlwaysOnPdn()
      → ApnContext.getApnSetting()
      → ApnContext.isProvisioningApn()
      → ApnContext.restartOnError()
      → ApnContext.getDataConnection()
      → ApnContext.getApnTypeBitmask()
      → ApnContext.getApnType()
      → ApnContext.markApnPermanentFailed()
      → ApnContext.setState()
      → <com.android.internal.telephony.dataconnection.DcTracker$Pro
      → DcTracker.getLinkProperties()
      → DcTracker.getCarrierConfig()
      → DcTracker.isPermanentFailure()
      → DcTracker.isPreferredApnMatchedExceptId()
      → DcTracker.shouldFallbackOnFailedHandover()
      → DcTracker.calculateNewRetryRequestType()
      → DcTracker.getCellLocationId()
      → DcTracker.requestTypeToString()
      → DcTracker.cancelReconnect()
      → DcTracker.checkDataRoamingStatus()
      → DcTracker.completeConnection()
      → DcTracker.hideProvisioningNotification()
      → DcTracker.log()
      → DcTracker.notifyLteAttachPdnListToRIL()
      → DcTracker.onDataSetupCompleteError()
      → DcTracker.sendHandoverCompleteMessages()
      → DcTracker.sendRestartRadio()
      → DcTracker.setDataProfilesAsNeeded()
      → DcTracker.setPreferredApn()
      → DcTracker.setRadio()
      → DcTracker.showProvisioningNotification()
      → DcTracker.update5gStatus()
      → ArrayList.iterator()
      → HashMap.containsKey()
      → HashMap.get()
      → HashMap.put()
      → Iterator.hasNext()
      → Iterator.next()
        → Notification.secSafeCharSequence()
        → <android.app.Notification$Style: void <init>()>
        → PersistableBundle.getIntArray()
        → CarrierConfigManager.getConfigForSubId()
        → <android.telephony.DataFailCause$$ExternalSyntheticLambda0: 
        → IntStream.anyMatch()
        → Map.getOrDefault()
        → TextUtils.isEmpty()
        → Map.get()
        → CharSequence.charAt()
        → CharSequence.length()
        → Intent.getAction()
        → CarrierSignalAgent.broadcast()
        → ArrayUtils.isEmpty()
        → SystemClock.elapsedRealtime()
        → LocalLog.append()
        → Duration.ofMillis()
        → Instant.now()
        → LocalDateTime.now()
        → SemFloatingFeature.getInstance()
        → SemFloatingFeature.getString()
        → SemTelephonyUtils.getMainOperator()
        → SemCarrierFeature.getInstance()
        → SemCarrierFeature.getString()
        → SystemProperties.get()
        → TelephonyFeatures.getSubOperatorName()
        → AlwaysOnPdn.isSupportingVoWIFI()
        → AlwaysOnPdn.isAlwaysOnPdnSupported()
        → AlwaysOnPdn.log()
        → AlwaysOnPdn.notifyAlwaysOnPdnToRIL()
        → ApnContext.log()
        → Resources.getString()
        → SparseIntArray.get()
        → SparseIntArray.put()
        → ApnContext.requestLog()
        → ApnContext.resetErrorCodeRetries()
        → RetryManager.markApnPermanentFailed()
        → RetryManager.getWaitingApns()
        → ArrayList.clear()
        → <android.content.BroadcastReceiver: void <init>()>
        → <android.net.LinkProperties: void <init>()>
        → DataConnection.getLinkProperties()
        → ConcurrentHashMap.get()
        → CarrierConfigManager.getDefaultConfig()
        → DataFailCause.isPermanentFailure()
        → DcTracker.skipCheckPermanentFailure()
        → AtomicBoolean.get()
        → ApnSetting.canHandleType()
        → ApnSetting.equalsExceptID()
        → CellIdentity.asCellLocation()
        → CdmaCellLocation.getBaseStationId()
        → GsmCellLocation.getCid()
        → Phone.getCurrentCellIdentity()
        → Handler.removeMessages()
        → ServiceState.getDataRoaming()
        → ApnContext.getState()
        → DataConnection.getConnectionParams()
        → DcTracker.getDataRoamingEnabledInternal()
        → Collection.iterator()
        → ConcurrentHashMap.values()
        → NotificationManager.cancel()
        → Context.startActivity()
        → Intent.makeMainSelectorActivity()
        → Intent.setData()
        → Intent.setFlags()
        → Uri.parse()
        → Handler.obtainMessage()
        → Handler.sendMessage()
        → DcTracker.loge()
        → DcTracker.startDataStallAlarm()
        → DcTracker.startNetStatPoll()
        → DcTracker.getNotificationManager()
        → Rlog.d()
        → Phone.invokeOemRilRequestRaw()
        → ByteArrayOutputStream.toByteArray()
        → <java.io.ByteArrayOutputStream: void <init>()>
        → ByteArrayOutputStream.close()
        → <java.io.DataOutputStream: void <init>(java.io.OutputStream)
        → DataOutputStream.close()
        → DataOutputStream.writeByte()
        → DataOutputStream.writeShort()
        → Phone.getAccessNetworksManager()
        → AccessNetworksManager.isInLegacyMode()
        → ApnContext.getDelayForNextApn()
        → ApnContext.getDelayForNextApn()
        → ApnContext.setDataConnection()
        → DcTracker.startReconnect()
        → DcTracker.sendHandoverCompleteMsg()
        → List.iterator()
        → List.clear()
        → Handler.obtainMessage()
        → ServiceState.getDataRoamingFromRegistration()
        → ApnSetting.equals()
        → ApnSetting.isEnabled()
        → ApnSetting.getApnSetId()
        → ApnSetting.getProfileId()
        → DataServiceManager.setDataProfile()
        → DcTracker.getPreferredApn()
        → DcTracker.createDataProfile()
        → DcTracker.createDataProfileEnabled()
        → DcTracker.setApnNeeded()
        → DcTracker.getPreferredApnSetId()
        → ArrayList.add()
        → ArrayList.contains()
        → ArrayList.containsAll()
        → ArrayList.isEmpty()
        → ArrayList.size()
        → <java.util.ArrayList: void <init>()>
        → DcTracker.ignoreForCDMA()
        → DcTracker.setPreferredApn()
        → TelephonyServiceManager$ServiceRegisterer.get()
        → TelephonyServiceManager.getTelephonyServiceRegisterer()
        → TelephonyFrameworkInitializer.getTelephonyServiceManager()
        → ITelephony$Stub.asInterface()
        → ITelephony.setRadio()
        → Notification$Builder.setChannelId()
        → Notification$Builder.setContentIntent()
        → Notification$Builder.setLocalOnly()
        → Notification$Builder.setOnlyAlertOnce()
        → <android.app.Notification$Builder: void <init>(android.conte
        → PendingIntent.getBroadcast()
        → Context.getColor()
        → Resources.getString()
        → TelephonyManager.getNetworkOperator()
        → ServiceState.getNrState()
          → Notification.removeTextSizeSpans()
          → CharSequence.subSequence()
          → CharSequence.toString()
          → ComponentName.getPackageName()
          → Context.getPackageManager()
          → Context.sendBroadcastAsUser()
          → Intent.getComponent()
          → Intent.addFlags()
          → Intent.setComponent()
          → <android.content.Intent: void <init>(android.content.Intent)
          → PackageManager.getApplicationInfo() [PKG_QUERY]
          → PackageManager.queryBroadcastReceivers()
          → SubscriptionManager.putSubscriptionIdExtra()
          → CarrierSignalAgent.createCompatIntent()
          → CarrierSignalAgent.log()
          → CarrierSignalAgent.loge()
          → List.isEmpty()
          → Set.iterator()
          → Collection.isEmpty()
          → Deque.add()
          → Deque.size()
          → Deque.remove()
          → SemFloatingFeature.loge()
          → Hashtable.get()
          → SemTelephonyUtils.getNetworkTypeCapability()
          → <com.samsung.android.feature.SemCarrierFeature: void <init>(
          → SemCarrierFeature.get()
          → Exception.printStackTrace()
          → SemTelephonyUtils.getSubOperator()
          → ImsManager.isWfcEnabledByPlatform()
          → ImsManager.getInstance()
          → AlwaysOnPdn.isAlwaysOnPdnRequiredByOperator()
          → DcTracker.isTmoSim()
          → DcTracker.isVzwSim()
          → Rlog.d()
          → Phone.getPhoneType()
          → AlwaysOnPdn.isDataDisabled()
          → SparseIntArray.get()
          → ContainerHelpers.binarySearch()
          → GrowingArrayUtils.insert()
          → Resources.getStringArray()
          → SparseIntArray.clear()
          → <android.net.LinkProperties: void <init>(android.net.LinkPro
          → PersistableBundle.getStringArray()
          → <android.telephony.DataFailCause$1: void <init>()>
          → ArrayUtils.contains()
          → <java.util.HashSet: void <init>()>
          → Map$Entry.getKey()
          → Map$Entry.getValue()
          → Map.entrySet()
          → Set.add()
          → Set.contains()
          → ApnSetting.hasApnType()
          → ApnSetting.defaultString()
          → ApnSetting.toStringExceptID()
          → ServiceState.getRoaming()
          → ServiceState.getDataRoamingType()
          → TelephonyFeatures.getSalesCode()
          → DataEnabledSettings.getDataRoamingEnabled()
          → <android.net.Uri$StringUri: void <init>(java.lang.String,and
          → Rlog.e()
          → AlarmManager.set()
          → Settings$Global.getInt()
          → SubscriptionManager.putPhoneIdAndSubIdExtra()
          → AlwaysOnPdn.isWifiConnected()
          → DcTracker$DataStallRecoveryHandler.isAggressiveRecovery()
          → DcTracker.isAnyDataConnected()
          → DcTracker.isPackageInstalled()
          → Phone.notifyDataActivity()
          → DcTracker.resetPollStats()
          → Runnable.run()
          → Context.createContextAsUser()
          → Rlog.log()
          → HalVersion.less()
          → Phone.getHalVersion()
          → TelephonyFacade.getTelephonyProperty()
          → RetryManager.getDelayForNextApn()
          → ApnContext.isFastRetryReason()
          → RetryManager.getDelayForNextApn()
          → Handler.obtainMessage()
          → Handler.sendMessageDelayed()
          → ApnContext.getReason()
          → Bundle.putBoolean()
          → Bundle.putInt()
          → Message.getData()
          → Message.sendToTarget()
          → Objects.equals()
          → IDataService.setDataProfile()
          → IDataServiceCallback$Stub.asBinder()
          → <com.android.internal.telephony.dataconnection.DataServiceMa
          → DataServiceManager.log()
          → DataServiceManager.loge()
          → DataServiceManager.sendCompleteMessage()
          → Map.put()
          → Map.remove()
          → ContentResolver.query()
          → Context.getContentResolver()
          → Cursor.moveToFirst()
          → Cursor.getColumnIndexOrThrow()
          → Cursor.getCount()
          → Cursor.getInt()
          → Cursor.close()
          → Uri.withAppendedPath()
          → ApnSetting.getNetworkTypeBitmask()
          → ApnSetting.getApnTypeString()
          → DcTracker.isCdmaApn()
          → DcTracker.fetchDunApns()
          → ArrayList.get()
          → ApnSetting.getApnTypeBitmask()
          → ApnSetting.getApnTypesStringFromBitmask()
          → DcTracker.createDataProfile()
          → DcTracker.getApnProfileID()
          → ServiceState.bearerBitmapHasCdma()
          → ApnSetting.isPersistent()
          → ApnSetting.getAuthType()
          → ApnSetting.getMtuV4()
          → ApnSetting.getMtuV6()
          → ApnSetting.getProtocol()
          → ApnSetting.getRoamingProtocol()
          → ApnSetting.getPassword()
          → ApnSetting.getUser()
          → DataProfile$Builder.build()
          → DataProfile$Builder.enable()
          → DataProfile$Builder.setApn()
          → DataProfile$Builder.setApnSetting()
          → DataProfile$Builder.setAuthType()
          → DataProfile$Builder.setBearerBitmask()
          → DataProfile$Builder.setMtuV4()
          → DataProfile$Builder.setMtuV6()
          → DataProfile$Builder.setPassword()
          → DataProfile$Builder.setPersistent()
          → DataProfile$Builder.setPreferred()
          → DataProfile$Builder.setProfileId()
          → DataProfile$Builder.setProtocolType()
          → DataProfile$Builder.setRoamingProtocolType()
          → DataProfile$Builder.setSupportedApnTypesBitmask()
          → DataProfile$Builder.setType()
          → DataProfile$Builder.setUserName()
          → <android.telephony.data.DataProfile$Builder: void <init>()>
          → ApnSetting.apnTypeBitmaskSame()
          → DcTracker.isApnSyncBlockedByLteTest()
          → DcTracker.isCellcomSim()
          → DcTracker.isNeedSetDataProfileForEUR()
          → Cursor.isClosed()
          → TelephonyFeatures.getCountryName()
          → DcTracker.getCdmaRatOrigin()
          → ContentResolver.insert()
          → ContentResolver.delete()
          → <android.content.ContentValues: void <init>()>
          → ContentValues.put()
          → ServiceManager.getService()
          → <android.os.TelephonyServiceManager$ServiceRegisterer: void 
            → <android.text.SpannableStringBuilder: void <init>(java.lang.
            → SpannableStringBuilder.setSpan()
            → Spanned.getSpanEnd()
            → Spanned.getSpanFlags()
            → Spanned.getSpanStart()
            → Spanned.length()
            → Spanned.getSpans()
            → CharacterStyle.getUnderlying()
            → TextAppearanceSpan.getLinkTextColor()
            → TextAppearanceSpan.getTextColor()
            → TextAppearanceSpan.getTextStyle()
            → TextAppearanceSpan.getFamily()
            → <android.text.style.TextAppearanceSpan: void <init>(java.lan
            → Intent.putExtra()
            → Intent.putExtra()
            → Intent.setAction()
            → Intent.getExtras()
            → Intent.getBooleanExtra()
            → Intent.getByteArrayExtra()
            → Intent.getIntExtra()
            → Intent.getStringExtra()
            → Bundle.keySet()
            → ApnSetting.getProtocolStringFromInt()
            → Rlog.i()
            → FeatureUtil.getLastFeatureVersion()
            → FeatureUtil.readSimCount()
            → SemCarrierFeature.loadCarrierFeatures()
            → SemCarrierFeature.loadDefaultFeatures()
            → <java.util.LinkedHashMap: void <init>()>
            → FeatureUtil.getCurrentCanonicalID()
            → FeatureUtil.getDefaultCanonicalID()
            → FeatureUtil.getLastCanonicalID()
            → SemCarrierFeature.isCarrierIdChanged()
            → SemCarrierFeature.isFeatureChanged()
            → Phone.getOperatorNumeric()
            → Phone.getDataEnabledSettings()
            → DataEnabledSettings.isUserDataEnabled()
            → DataFailCause$1.add()
            → ArrayUtils.indexOf()
            → TelephonyManager.convertNetworkTypeBitmaskToString()
            → ApnSetting.UriToString()
            → ApnSetting.portToString()
            → TextUtils.join()
            → Settings$Secure.getInt()
            → GlobalSettingsHelper.getBoolean()
            → DataEnabledSettings.getDefaultDataRoamingEnabled()
            → DataEnabledSettings.log()
            → <android.net.Uri$StringUri: void <init>(java.lang.String)>
            → AlarmManager.legacyExactLength()
            → AlarmManager.setImpl()
            → DcTracker$DataStallRecoveryHandler.getRecoveryAction()
            → DataConnection.isActive()
            → HashMap.values()
            → PackageManager.getPackageInfo() [PKG_QUERY]
            → HalVersion.compareTo()
            → TelephonyManager.getTelephonyProperty()
            → <android.os.Bundle: void <init>()>
            → <android.telephony.data.IDataServiceCallback$Stub: void <ini
            → Uri$Builder.build()
            → Uri$Builder.appendEncodedPath()
            → Uri.buildUpon()
            → DcTracker.bitmaskHasTech1xEvdo()
            → ServiceState.isCdma()
            → ServiceState.rilRadioTechnologyToNetworkType()
            → ApnSetting$Builder.build()
            → ApnSetting$Builder.setApnName()
            → ApnSetting$Builder.setApnTypeBitmask()
            → ApnSetting$Builder.setCarrierEnabled()
            → ApnSetting$Builder.setEntryName()
            → ApnSetting$Builder.setOperatorNumeric()
            → ApnSetting$Builder.setProtocol()
            → <android.telephony.data.ApnSetting$Builder: void <init>()>
            → ApnSetting.canSupportNetworkType()
            → DcTracker.getPreferredApnFromDB()
            → DcTracker.isKddiApn()
            → DcTracker.isKddiDunApn()
            → DcTracker.isPreferredApnUserEdited()
            → DcTracker.getDataRat()
            → DcTracker.getOperatorForCreateAllApnList()
            → <java.util.ArrayList: void <init>(int)>
            → ArrayList.add()
            → <java.util.HashMap: void <init>()>
            → TextUtils.join()
            → List.add()
            → Map.keySet()
            → ApnSetting$Builder.setAuthType()
            → ApnSetting$Builder.setModemCognitive() [APPOPS]
            → ApnSetting$Builder.setMtuV4()
            → ApnSetting$Builder.setMtuV6()
            → ApnSetting$Builder.setNetworkTypeBitmask()
            → ApnSetting$Builder.setPassword()
            → ApnSetting$Builder.setProfileId()
            → ApnSetting$Builder.setRoamingProtocol()
            → ApnSetting$Builder.setUser()
            → <android.telephony.data.DataProfile: void <init>(android.tel
            → <java.lang.IllegalArgumentException: void <init>(java.lang.S
            → <java.io.File: void <init>(java.lang.String)>
            → FileInputStream.read()
            → <java.io.FileInputStream: void <init>(java.io.File)>
            → FileInputStream.close()
            → SemTelephonyUtils.getCountry()
            → TelephonyFeatures.isMainOperatorSpecific()
            → DcTracker.is1xEvdo()
            → <android.util.ArrayMap: void <init>()>
            → ArrayMap.put()
              → <android.text.style.MetricAffectingSpan: void <init>()>
              → SemSystemProperties.get()
              → FeatureUtil.getReadablePhoneIDName()
              → NumberFormatException.printStackTrace()
              → SemSystemProperties.getInt()
              → FeatureUtil.getCarrierFeature()
              → SemCarrierFeature.setSlotTargetFeatureList()
              → SemCarrierFeature.setSlotDefaultFeatureList()
              → TelephonyProperties.mobile_data()
              → DataEnabledSettings.IgnoreDataEnabledOnRoaming()
              → DataEnabledSettings.isStandAloneOpportunistic()
              → Optional.orElse()
              → Uri.toString()
              → GlobalSettingsHelper.getSettingName()
              → <android.net.Uri$AbstractHierarchicalUri: void <init>(androi
              → <java.lang.NullPointerException: void <init>(java.lang.Strin
              → AlarmManager.setImpl()
              → <android.os.HandlerExecutor: void <init>(android.os.Handler)
              → Settings$System.getInt()
              → <com.android.internal.telephony.dataconnection.DcTracker: an
              → StateMachine.getCurrentState()
              → <android.os.Binder: void <init>()>
              → IDataServiceCallback$Stub.attachInterface()
              → Uri$Builder.hasSchemeOrAuthority()
              → <android.net.Uri$HierarchicalUri: void <init>(java.lang.Stri
              → <android.net.Uri$OpaqueUri: void <init>(java.lang.String,and
              → Uri$PathPart.makeAbsolute()
              → <java.lang.UnsupportedOperationException: void <init>(java.l
              → Uri$Builder.path()
              → Uri$PathPart.appendEncodedSegment()
              → ServiceState.convertNetworkTypeBitmaskToBearerBitmask()
              → <android.telephony.data.ApnSetting: void <init>(android.tele
              → ServiceState.bitmaskHasTech()
              → ApnSetting.makeApnSetting()
              → DcTracker.getPreferredApnCursor()
              → NetworkRegistrationInfo.getAccessNetworkTechnology()
              → ServiceState.getNetworkRegistrationInfo()
              → ServiceState.networkTypeToRilRadioTechnology()
              → TelephonyManager.getTelephonyProperty()
              → TelephonyFeatures.getNtcFeature()
              → TelephonyFeatures.getSimbasedChangeType()
              → Iterable.iterator()
              → <android.telephony.data.DataProfile: void <init>(android.tel
              → <android.util.ArrayMap: void <init>(int,boolean)>
              → ArrayMap.indexOf()
              → ArrayMap.indexOfNull()
              → ArrayMap.allocArrays()
              → ArrayMap.freeArrays()
              → <java.util.ConcurrentModificationException: void <init>()>

