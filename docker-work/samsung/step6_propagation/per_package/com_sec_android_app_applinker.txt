######################################################################
# Propagation: com.sec.android.app.applinker
######################################################################

JARs:                 services
Classes:              3
Occurrences:          3
Usage types:          invoke
Enforcement patterns: early_return
Security tags:        APPOPS, UID_CHECK

────────────────────────────────────────────────────────────
Occurrence #1
────────────────────────────────────────────────────────────
  JAR:      services
  Class:    com.samsung.accessory.manager.authentication.cover.CoverAuthenticator
  Method:   <com.samsung.accessory.manager.authentication.cover.CoverAuthenticator: void setCoverVerified(boolean,boolean,com.samsung.accessory.manager.authentication.cover.CoverInfo,com.samsung.accessory.manager.authentication.AuthenticationResult)>
  Line:     4845
  Register: v8 := "com.sec.android.app.applinker"
  Usage:    [invoke] invoke-virtual {v3, v8}, Landroid/content/Intent;->setPackage(Ljava/lang/String;)Landroid/content/In
  Patterns: early_return
  Flow:     [invoke] android.content.Intent.setPackage() → [other] iget-object v8, v0, Lcom/samsung/accessory/manager/authentic → [b

  CALLERS (who invokes this method):
      ← <com.samsung.accessory.manager.authentication.cover.CoverAut
      ← CoverAuthenticator.handleAuthResponse()
        ← CoverAuthenticator$10.run()
        ← CoverAuthenticator$CoverAuthHandler.handleMessage()
        ← <com.samsung.accessory.manager.authentication.cover.CoverAut

  CALLEES (what this method invokes):
      → Context.sendBroadcast()
      → Context.sendBroadcastAsUser()
      → Intent.putExtra()
      → Intent.putExtra()
      → Intent.setFlags()
      → Intent.setPackage()
      → <android.content.Intent: void <init>(java.lang.String)>
      → PowerManager.setCoverType()
      → SystemClock.elapsedRealtime()
      → InputManagerService.setCoverVerify()
      → AuthenticationResult.getByteArrayExtraData()
      → AuthenticationResult.getByteArrayManagerURI()
      → AuthenticationResult.getExtraId()
      → AuthenticationResult.getStringManagerURI()
      → <com.samsung.accessory.manager.authentication.cover.CoverAut
      → CoverAuthenticator.applyCoverVerifiedState()
      → CoverAuthenticator.isNeedConvertClearCoverType()
      → CoverAuthenticator.notifyFriendsStateChanged()
      → CoverAuthenticator.sendToIssManager()
      → CoverAuthenticator.startAuthAlarm()
      → CoverBroadcaster.broadcastCoverAttachStatus()
      → CoverBroadcaster.setRealCoverType()
      → CoverInfo.getId()
      → CoverInfo.getColor()
      → CoverInfo.getModel()
      → CoverInfo.getSmapp()
      → CoverInfo.getType()
      → CoverInfo.getUrl()
      → CoverInfo.getSn()
      → CoverInfo.setType()
      → CoverState.getType()
      → <com.samsung.android.cover.CoverState: void <init>()>
      → CoverState.setAttachState()
      → CoverState.setColor()
      → CoverState.setFakeCover()
      → CoverState.setFriendsType()
      → CoverState.setModel() [APPOPS]
      → CoverState.setSerialNumber()
      → CoverState.setSmartCoverAppUri()
      → CoverState.setSmartCoverCookie()
      → CoverState.setType()
        → Binder.getCallingUid() [UID_CHECK]
        → NativeInputManagerService.setCoverVerify()
        → CoverAuthenticator$CoverAuthStateFile.writeFile()
        → LocalServices.getService()
        → SemUnionManagerLocal.notifySmartCoverAttachStateChanged()
        → Context.getPackageManager()
        → PackageManager.hasSystemFeature()
        → <android.content.ComponentName: void <init>(java.lang.String
        → Intent.putExtra()
        → Intent.setComponent()
        → <android.content.Intent: void <init>()>
        → CoverAuthenticator.sendFriendsCoverAttachInformation()
        → SemUnionManagerLocal.accessoryStateChanged()
        → Context.sendBroadcast()
        → Intent.setAction()
        → CoverAuthenticator.isPackageExist()
        → AlarmManager.set()
        → SecureRandom.nextInt()
        → <java.security.SecureRandom: void <init>()>
        → Intent.putExtra()
        → <android.graphics.Rect: void <init>()>
        → CoverState.updateVisibleRect()
          → <com.samsung.accessory.manager.authentication.cover.CoverAut
          → Thread.start()
          → ArrayMap.get()
          → PackageManager.getInstalledApplications()
          → Iterator.hasNext()
          → Iterator.next()
          → List.iterator()
          → AlarmManager.legacyExactLength()
          → AlarmManager.setImpl()
          → Resources.getSystem()
          → Resources.getDisplayMetrics()
          → Resources.getDimensionPixelSize()
          → Rect.setEmpty()
          → CoverState.getDefaultDensity()
            → <java.lang.Thread: void <init>(java.lang.String)>
            → ArrayMap.indexOfKey()
            → AlarmManager.setImpl()
            → <android.os.HandlerExecutor: void <init>(android.os.Handler)
              → ArrayMap.indexOf()
              → ArrayMap.indexOfNull()
              → <android.app.AlarmManager$ListenerWrapper: void <init>(andro
              → AlarmManager$ListenerWrapper.setExecutor()
              → IAlarmManager.set()
              → RemoteException.rethrowFromSystemServer()
              → WeakReference.get()
              → <java.lang.ref.WeakReference: void <init>(java.lang.Object)>
              → WeakHashMap.get()
              → WeakHashMap.put()
              → <java.util.WeakHashMap: void <init>()>
              → Preconditions.checkNotNull()

────────────────────────────────────────────────────────────
Occurrence #2
────────────────────────────────────────────────────────────
  JAR:      services
  Class:    com.samsung.accessory.manager.authentication.cover.RestrictionPolicyOberver
  Method:   <com.samsung.accessory.manager.authentication.cover.RestrictionPolicyOberver: void showFelicaNotification()>
  Line:     338
  Register: v1 := "com.sec.android.app.applinker"
  Usage:    [invoke] invoke-virtual {v0, v1}, Landroid/content/Intent;->setPackage(Ljava/lang/String;)Landroid/content/In
  Patterns: early_return
  Flow:     [invoke] android.content.Intent.setPackage() → [reassigned] v1 overwritten

  CALLERS (who invokes this method):
      ← CoverAuthenticator$4.run()

  CALLEES (what this method invokes):
      → Context.sendBroadcastAsUser()
      → Intent.setFlags()
      → Intent.setPackage()
      → <android.content.Intent: void <init>(java.lang.String)>

────────────────────────────────────────────────────────────
Occurrence #3
────────────────────────────────────────────────────────────
  JAR:      services
  Class:    com.android.server.sepunion.cover.CoverServiceManager
  Method:   <com.android.server.sepunion.cover.CoverServiceManager: void bindCoverService(int,boolean)>
  Line:     888
  Register: p2 := "com.sec.android.app.applinker"
  Usage:    [invoke] invoke-virtual {p1, p2}, Landroid/content/Intent;->setPackage(Ljava/lang/String;)Landroid/content/In
  Patterns: early_return
  Flow:     [invoke] android.content.Intent.setPackage() → [reassigned] p2 overwritten

  CALLERS (who invokes this method):
      ← CoverManagerServiceImpl.onUserUnlocked()
      ← CoverManagerServiceImpl.sendCoverAttachStateLocked()
      ← CoverManagerServiceImpl.sendDataToNfcLedCover()
      ← CoverServiceManager$1.onReceive()
      ← CoverServiceManager.updateCoverState()
        ← SemPluginManagerService.onUnlockUser()
        ← SemPluginManagerService.onUserUnlocked()
        ← CoverManagerServiceImpl.updateCoverAttachState()
        ← CoverManagerServiceImpl.updateCoverSwitchState()
        ← SemPluginLocalService.sendDataToNfcLedCover()
        ← CoverManagerServiceImpl$1.run()
        ← CoverManagerServiceImpl$2.run()
        ← CoverManagerServiceImpl.sendCoverSwitchStateLocked()
        ← CoverManagerServiceImpl.updateCoverWindowSize()
          ← <com.android.server.sepunion.cover.CoverManagerServiceImpl: 
          ← CoverManagerServiceImpl.initialize()
          ← CoverManagerServiceImpl.notifyCoverAttachStateChangedInternal()
          ← CoverManagerServiceImpl.notifySmartCoverAttachStateChangedInternal()
          ← CoverManagerServiceImpl.notifyCoverSwitchStateChanged()
          ← <com.android.server.sepunion.cover.CoverManagerServiceImpl: 
          ← <com.android.server.sepunion.cover.CoverManagerServiceImpl: 
            ← CoverManagerServiceImpl$5.onCoverTestModeChanged()
            ← CoverManagerServiceImpl$6.run()
            ← CoverManagerServiceImpl.systemRunning()
            ← CoverManagerServiceImpl.notifyCoverAttachStateChanged()
            ← <com.android.server.sepunion.cover.CoverManagerServiceImpl: 
            ← CoverManagerServiceImpl.notifySmartCoverAttachStateChanged()
            ← SemPluginManagerService.dumpCoverInfomation()
            ← SemPluginManagerService.notifyCoverSwitchStateChanged()
            ← CoverManagerServiceImpl$3.onDisplayFoldChanged()
            ← CoverManagerServiceImpl$7.onSensorChanged()
            ← CoverManagerServiceImpl.dump()
            ← CoverManagerServiceImpl$10.run()
            ← CoverManagerServiceImpl$CoverManagerHandler.handleMessage()
              ← SemPluginManagerService.onBootPhase()
              ← SemPluginManagerService.notifySmartCoverAttachStateChanged()
              ← SemPluginManagerService.dump()
              ← SemUnionMainServiceImpl$1.notifyCoverSwitchStateChanged()
              ← SemPluginLocalService.dump()

  CALLEES (what this method invokes):
      → ActivityManager.getCurrentUser()
      → ComponentName.equals()
      → ComponentName.getPackageName()
      → Context.sendBroadcast()
      → Intent.setPackage()
      → <android.content.Intent: void <init>(java.lang.String)>
      → <android.os.UserHandle: void <init>(int)>
      → CoverServiceManager.getCoverServiceNameLocked()
      → CoverServiceManager.getPredefinedCoverServiceNameLocked()
      → CoverServiceManager.getSystemUICoverService()
      → CoverServiceManager.bindCoverServiceLocked()
      → CoverServiceManager.isShouldNotBindCoverService()
      → CoverServiceManager.registerBroadcastReceiver()
      → Log.d()
        → SparseArray.size()
        → SparseArray.get()
        → CoverServiceManager.refreshCoverServicesLocked()
        → Feature.isNfcAuthEnabled()
        → Feature.getInstance()
        → Context.bindServiceAsUser()
        → Intent.setComponent()
        → <android.content.Intent: void <init>()>
        → <com.android.server.sepunion.cover.CoverServiceManager$2: vo
        → CoverServiceManager.containsBindingServiceLocked()
        → CoverServiceManager.addBindingServiceLocked()
        → CoverServiceManager.updateBindHistoryLocked()
        → Log.e()
        → Log.e()
        → CoverServiceManager.verifySystemFeature()
        → CoverTestModeUtils.getTestCoverType()
        → Context.registerReceiverAsUser()
        → <android.content.IntentFilter: void <init>()>
        → IntentFilter.addAction()
        → IntentFilter.addDataScheme()
        → IntentFilter.addDataSchemeSpecificPart()
        → Debug.semIsProductDev()
        → Log.getCodeInfoString()
          → SparseArray.gc()
          → SparseArray.get()
          → SparseArray.keyAt()
          → SparseArray.valueAt()
          → SparseArray.clear()
          → SparseArray.put()
          → CoverServiceManager.queryInstalledCoverServices()
          → <com.android.server.sepunion.cover.Feature: void <init>(andr
          → HashMap.keySet()
          → Set.contains()
          → HashMap.put()
          → CoverServiceManager.toTimestampFormat()
          → Feature.isSupportClearCameraViewCover()
          → Feature.isSupportClearCover()
          → Feature.isSupportClearSideViewCover()
          → Feature.isSupportFlipCover()
          → Feature.isSupportGamePackCover()
          → Feature.isSupportLEDBackCover()
          → Feature.isSupportMiniSviewWalletCover()
          → Feature.isSupportNeonCover()
          → Feature.isSupportNfcLedCover()
          → StackTraceElement.toString()
          → StringBuffer.toString()
          → StringBuffer.append()
          → <java.lang.StringBuffer: void <init>(java.lang.String)>
          → Thread.getStackTrace()
          → Thread.currentThread()
            → ContainerHelpers.binarySearch()
            → <java.lang.ArrayIndexOutOfBoundsException: void <init>(int)>
            → GrowingArrayUtils.insert()
            → GrowingArrayUtils.insert()
            → Binder.clearCallingIdentity()
            → Binder.restoreCallingIdentity()
            → CoverServiceManager.getVerifiedCoverService()
            → CoverServiceManager.queryIntentService()
            → Feature.getSystemProperties()
            → Feature.updateDeviceSupportFeature()
            → Feature.updateSystemFeature()
            → Calendar.get()
            → Calendar.getInstance()
              → <android.content.ComponentName: void <init>(java.lang.String
              → Bundle.getInt()
              → <android.util.SparseArray: void <init>()>
              → Log.w()
              → Iterator.hasNext()
              → Iterator.next()
              → List.size()
              → List.iterator()
              → Context.getPackageManager()
              → PackageManager.queryIntentServicesAsUser()
              → SystemProperties.get()
              → CoverManagerUtils.isFileExists()
              → PackageManager.hasSystemFeature()
              → SemFloatingFeature.getInstance()
              → SemFloatingFeature.getInteger()

